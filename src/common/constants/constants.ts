export const BASE_PATH = "/v1";
export const ACCESS_TOKEN_LIFETIME = 1000 * 60 * 10; // 10 minutes
export const SERVICE_HOME_PATH = "https://bluebirdfs.com";

export const enum QueueName {
  DEFAULT = "DEFAULT",
  SERVICE_METRICS = "METRICS",
}
